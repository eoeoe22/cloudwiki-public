---
// 공통 사이드바 컴포넌트. public/components/sidebar.html 을 단일 소스로 ?raw import 하여 인라인.
// 런타임 마커(.app-wiki-name, .wiki-logo-container, #custom-sidebar-content)는 set:html 로 보존되며,
// #custom-sidebar-content 는 런타임 applyPageSSR 가 env SIDEBAR 기반으로 채우거나 제거한다.
import raw from '../../../public/components/sidebar.html?raw';
---
<Fragment set:html={raw} />
