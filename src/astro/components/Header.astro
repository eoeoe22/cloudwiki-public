---
// 공통 헤더 컴포넌트. public/components/header.html 을 단일 소스로 ?raw import 하여
// 빌드 타임에 그대로 인라인한다(손으로 복제하지 않음 — 비-Astro 페이지도 같은 파일을 런타임 주입으로 사용).
// 인라인 <script>(검색 자동완성)·onclick 핸들러·런타임 마커(.app-wiki-name, .wiki-logo-container)는
// set:html 로 원문 그대로 출력되어 정상 동작한다.
import raw from '../../../public/components/header.html?raw';
---
<Fragment set:html={raw} />
